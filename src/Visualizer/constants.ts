export const AUDIO_DATA_LENGTH = 1024;

export const WINDOW_WIDTH = window.innerWidth;

export const WINDOW_HEIGHT = window.innerHeight;

export const WINDOW_CENTER = {
  X: WINDOW_WIDTH / 2,
  Y: WINDOW_HEIGHT / 2
};
